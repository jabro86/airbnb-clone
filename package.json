{
  "private": true,
  "scripts": {
    "build:server": "lerna run build --scope={@abb/common,@abb/server}"
  },
  "workspaces": [
    "packages/*"
  ],
  "name": "abb",
  "devDependencies": {
    "lerna": "^3.10.6"
  }
}
